# This workflow is DISABLED during architecture pivot (Week 4-5)
# Old workflow tested Manta parachain node which we've deprecated
# New workflow will be created after standalone node is built
#
# See: docs/architecture-pivot.md for context
#
# DO NOT ENABLE until standalone node is ready

name: Build and Test Chameleon (DISABLED)

on:
  push:
    branches: [ develop, main ]
  pull_request:
    branches: [ develop, main ]

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Architecture Pivot in Progress
        run: |
          echo "⚠️  Old workflow disabled during architecture pivot"
          echo "📖 See docs/architecture-pivot.md for details"
          echo "✅ New standalone node workflow will replace this"
          echo ""
          echo "Week 4-5: Building Substrate standalone node"
          echo "Week 6+: New workflow will test standalone architecture"
