[package]
name = "pallet-chameleon-mev"
version = "0.1.0"
edition = "2021"
description = "Pallet implementing MEV protection for Chameleon Network."
license = "GPL-3.0"
readme = "README.md"

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "doc_cfg"]

[features]
default = ["std"]

runtime-benchmarks = [
  "frame-benchmarking/runtime-benchmarks",
  "frame-support/runtime-benchmarks",
  "frame-system/runtime-benchmarks",
]

try-runtime = [
  "frame-support/try-runtime",
  "frame-system/try-runtime",
]

std = [
  "frame-benchmarking?/std",
  "frame-support/std",
  "frame-system/std",
  "codec/std",
  "scale-info/std",
  "sp-core/std",
  "sp-runtime/std",
  "sp-std/std",
  "sha3/std",
]

[dependencies]
codec = { package = "parity-scale-codec", version = "3.6", default-features = false, features = ["derive"] }
scale-info = { version = "2.11", default-features = false, features = ["derive"] }
frame-benchmarking = { version = "31", default-features = false, optional = true }
frame-support = { version = "31", default-features = false }
frame-system = { version = "31", default-features = false }
sp-core = { version = "31", default-features = false }
sp-runtime = { version = "34", default-features = false }
sp-std = { version = "14", default-features = false }
sha3 = { version = "0.10", default-features = false }

[dev-dependencies]
sp-core = { version = "31", features = ["std"] }
sp-io = { version = "33", features = ["std"] }
pallet-balances = { version = "31", features = ["std"] }
pallet-timestamp = { version = "30", features = ["std"] }
